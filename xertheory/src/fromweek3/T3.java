package fromweek3;

/*
            Will it compile? If so will it run without exception?
            If so what is printed? Try to predict!

 */
public class T3 {

    public static void main(String args[]) {
        new T3().program();
    }

    void program() {
        /*
        Integer[] is = {1, 2, 3};         // Also try Number[]
        upDate(is);
        out.println(Arrays.toString(is));

         */
    }
    
    public static void upDate(Object[] os) {
        /*
        os[0] = null;
        os[1] = 1;
        os[2] = 1.2;
        */
    }
}
